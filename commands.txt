docker logs progetto-pa-dbpg-1 


docker exec -it progetto-pa-dbpg-1  psql -U postgres prga

//sudo docker run --rm --name prga -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres
//sudo docker exec -it progx_dbpg_1 psql -U postgres prga


SELECT * FROM "users";

SELECT * FROM "orders";

SELECT * FROM "store";



CREATE TABLE cargo (
  food VARCHAR(255) NOT NULL,
  quantity INTEGER NOT NULL,
  index TIMESTAMP,
  orderId UUID REFERENCES orders(uuid)
)

-- Creazione della colonna orderId nella tabella FoodItemOrder
CREATE TABLE fooditemorder (
  foodIndex SERIAL PRIMARY KEY,
  food VARCHAR(255) NOT NULL,
  quantity INTEGER NOT NULL,
  orderId UUID REFERENCES orders(uuid) -- Chiave esterna che fa riferimento all'ID dell'ordine
);

